# Database
DATABASE_URL="postgresql://user:password@localhost:5432/vhl_recruitment?schema=public"

# Cron Secret (for securing API endpoints)
CRON_SECRET="your-secret-key-here"

# Optional: Playwright storage state (base64 encoded) for authentication
PLAYWRIGHT_STORAGE_STATE_B64=""

# Node environment
NODE_ENV="development"
